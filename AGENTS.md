# Agents Guide

このPWAは、千歳青年会議所メンバー向けポータルです。
作業するエージェントは、機能追加よりも「安全な運用」と「引き継ぎやすさ」を優先してください。

## 目的

- スマートフォンから主要リンクへすぐアクセスできるようにする。
- JC学習、議案チェック、予定・出欠、資料置き場、例会コンテンツを整理する。
- 次年度担当者がリンク差し替えと年度更新を迷わずできるようにする。

## セキュリティと掲載ルール

- 認証情報、パスワード、Cookie、トークン、ログイン情報は絶対に保存しない。
- 公開範囲が不明なDrive/Sheets/Docsは、リンク追加前に管理者確認にする。
- 個人情報や未公開資料の本文をPWAに直接書かない。
- 外部公式資料は本文転載せず、短い説明文とリンクだけにする。
- 4月AI例会の既存コンテンツは削除しない。アーカイブ導線として残す。

## 編集の優先順位

1. `data/portal-data.json` の更新で対応する。
2. リンク台帳が必要なら `data/link-inventory.csv` も更新する。
3. 運用説明が変わるなら `docs/` を更新する。
4. 画面そのものが必要な場合だけ `styles.css` / `app.js` / `index.html` を触る。

## UI/UXの判断基準

- トップページは入口に徹する。
- 重要リンクは上部に置く。
- スマホ片手操作を優先し、主要5ページは下部ナビに固定する。
- 文言は短く、カード名は一目で分かるものにする。
- 未設定リンクは隠さず「リンク未設定」と表示する。
- 年度更新が必要なリンクは説明文や台帳で分かるようにする。

## チェック

変更後は次を実行します。

```bash
npm run check
```

ローカル確認は次です。

```bash
npm run serve
```

確認URL:

```text
http://127.0.0.1:4177/
```

## 完了前に見る資料

- `README.md`
- `Design.md`
- `Desing.md`
- `docs/handover-guide.md`
- `docs/annual-update-checklist.md`
- `docs/content-management.md`
- `docs/publishing-options.md`
